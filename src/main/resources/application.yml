spring:
  datasource:
    url: jdbc:mysql://localhost:3306/apollo
    username: root
    password: 'qwer1234'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    jpa:
      hibernate:
        ddl-auto: create
      properties:
        hibernate:
          show_sql: true
          format_sql: true
          dialect: org.hibernate.dialect.MySQL8Dialect